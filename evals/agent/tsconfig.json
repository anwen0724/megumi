{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": "../.."
  },
  "include": [
    "./**/*.ts",
    "../../packages/**/*.ts"
  ],
  "exclude": [
    "../../node_modules",
    "../../apps",
    "../../tests"
  ]
}
