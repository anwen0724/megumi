# @megumi/input

Owns input sensing facts and RawInput to ParsedInput normalization.

This package represents user text, attachments, references, selected files/ranges, artifact references, input targets, metadata, and explicit command facts. It does not interpret user intent, advance Agent Runs, execute Tool Calls, decide permissions, persist session history, render input UI, or use Electron IPC.
