# @megumi/command

Owns explicit command entrypoints for Megumi.

This package defines command definitions, slash command parsing, command registries, dispatch handoff results, and audit facts. It does not execute tools, shell commands, Agent Runs, workspace writes, permission decisions, Electron IPC, or UI autocomplete rendering.
