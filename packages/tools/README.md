# @megumi/tools

Owns future tool contracts, registry interfaces, executor contracts, built-in tool definitions, and MCP integration boundaries for Megumi.

Tool implementations may depend on platform-independent security policy through `@megumi/security`, but must not depend on Electron renderer state.
