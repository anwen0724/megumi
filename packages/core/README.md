# @megumi/core

Owns platform-independent runtime orchestration contracts and core flows for Megumi.

This package should stay free of Electron, renderer, SQLite repository, and concrete provider adapter dependencies. Desktop-specific composition belongs in `apps/desktop/src/main/services`.
