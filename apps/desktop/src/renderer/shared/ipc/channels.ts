/*
 * Renderer-side re-export of desktop IPC channel names.
 */
export { IPC_CHANNELS, isIpcChannel } from '@megumi/desktop/main/ipc/channels';
export type { IpcChannel } from '@megumi/desktop/main/ipc/channels';
