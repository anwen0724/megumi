# Scripts

This directory is for repository-level automation scripts.

Use it for:

- migration helpers
- code generation helpers
- release or packaging checks
- cleanup scripts
- one-off maintenance scripts that are useful enough to keep

Do not put runtime business logic here. Code used by the application at runtime belongs in `apps/` or `packages/`.
