# 旧 Python 后端测试

归档日期：2026-09-20。按用户要求原样归档，不复用、不保留兼容层。

保留原相对目录结构和未提交内容；archive-manifest.json 记录移动前文件大小与 SHA-256，移动后已经逐文件校验一致。归档包含原目录中的既有缓存。

后续实现与测试以 docs/develop/python-backend-refactor/ 下的有效 Spec 为依据。归档内容不参与常规运行与检查。恢复时可按 manifest 相对路径移回上两级的原目录；已有新文件时不得覆盖。
